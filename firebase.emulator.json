{
  "//": "Config SOLO para el Emulator Suite local. Usa reglas PERMISIVAS (firestore.rules.emulator) para que el seed de la app pueda sembrar. NUNCA se usa para deploy: el deploy a la nube usa firebase.json -> firestore.rules (estrictas). Arrancar con: firebase emulators:start --config firebase.emulator.json --only auth,firestore",
  "firestore": {
    "rules": "firestore.rules.emulator",
    "indexes": "firestore.indexes.json"
  },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": true, "port": 4000 },
    "singleProjectMode": true
  }
}
